# DeskPad — 프로젝트 규칙 (CLAUDE.md)

이 파일은 `E:\deskpad` 프로젝트에서 작업하는 모든 Claude 세션이 항상 지켜야 하는 규칙이다.
새 세션이 시작될 때마다 이 문서와 `roadmap.md`(진행 상황 기록)를 먼저 읽고 시작한다.

## 1. 프로젝트 개요

- iPad Pro 1세대(구형) 책상 거치용 상시 디스플레이 웹앱. React + TypeScript + Vite, 정적 사이트.
- GitHub `main` 브랜치 ↔ Netlify 자동 배포 연결됨(로컬 프로젝트 경로: `E:\deskpad`).
- 화면/UX 제약(전부 계속 유지, 요청 없이 바꾸지 않는다):
  - iPad 가로모드, `100vw`/`100vh`(또는 `100%`) 뷰포트에 딱 맞춤. 페이지 스크롤 없음.
  - 다크 테마, 멀리서도 읽히는 큰 글씨.
  - 구형 iPad Safari에서 가볍게 동작해야 함(불필요한 애니메이션 금지).
  - 외부 UI/차트/아이콘/캘린더 라이브러리 금지 — 순수 React + CSS, 아이콘은 이모지, 차트는 손으로 그린 SVG.
- 화면 모드 2가지(고정 구조, `roadmap.md` 14~15번 참고): 기본 모드(정보모드, Dashboard 상시 표시) / 사진 모드(디지털 액자, 탭하면 Dashboard 임시 표시 후 자동 복귀).
- Market(주가지수) 데이터는 사용자가 명시적으로 재개 지시하기 전까지 완전히 동결 상태 — 관련 코드에 먼저 손대지 않는다.

## 2. 코드 작업 시 공통 절차

- 수정 전: 기존 코드/호출 관계/데이터 흐름을 먼저 파악하고, 작업 목적·수정 파일·영향 범위·위험 요소를 정리한다.
- 요청하지 않은 대규모 리팩터링이나 구조 변경은 하지 않는다. DB/스키마/공통 계산 로직을 바꿀 때는 실행 전에 이유와 영향 범위를 설명한다.
- 작업 단위마다: `npm run build`로 TypeScript 에러 0건 확인 → (Netlify Functions를 건드렸다면) `npx tsc --noEmit -p netlify/functions/tsconfig.json`도 확인 → 관련 있으면 Playwright로 동작 검증.
- 완료 후 변경 파일은 `SendUserFile`로 전달하고 `mcp__remote-devices__device_commit_files`로 `E:\deskpad`에 반영, `roadmap.md`(project memory)에 완료 항목/변경 이유/구현 내용/검증 결과를 기존 형식대로 기록한다(이 roadmap 기록 자체는 상세하게 — 아래 보고 형식은 사용자에게 보여주는 채팅 응답에만 적용).

### 2-1. 작업 완료 후 채팅 보고 형식 (매 단위 작업마다)

작업 완료 후 보고는 최대 5줄로 간단히 한다.

```
변경: 핵심 수정 1~2줄
테스트: build 및 주요 테스트 결과
상태: 로컬/Git/배포 여부
문제: 남은 문제 있을 때만
다음: 사용자가 확인할 것 1줄
```

변경 파일 전체 목록, 테스트 파일명, 세부 구현 과정은 사용자가 요청할 때만 설명한다. (이 절은 "작업 마감" 시 3-9 최종 보고 형식과는 별개 — 평소 매 작업 완료 보고에 적용한다.)

### 2-2. 개발 속도/세션 사용량 절약 원칙 (2026-08-17 도입)

작은 수정 하나에도 매번 전체 회귀 테스트(verify4~20 전부)를 돌리는 습관이 세션 자원을 과도하게
쓰고 있다고 사용자가 지적함(다른 프로젝트 때보다 체감 속도가 느려진 원인으로 지목). 아래 원칙을
앞으로 기본으로 적용한다:

1. 작은 수정마다 전체 회귀 테스트를 실행하지 않는다.
2. 수정한 기능과 직접 관련된 테스트만 먼저 실행한다.
3. `npm run build`/`tsc`는 기능 단위 작업 완료 시 한 번 실행한다.
4. 전체 regression suite는 "오늘 작업 마감" 직전 또는 여러 파일에 걸친 중요한 구조 변경(예: 훅을
   상위 컴포넌트로 끌어올리는 등) 직후에만 실행한다.
5. 이미 조사한 파일을 불필요하게 다시 전수 조사하지 않는다.
6. 작업 중간 진행 상황("지금 ~하는 중입니다" 등)을 장문으로 계속 보고하지 않는다 — 완료 후
   2-1번 형식으로만 짧게 보고한다.
7. `roadmap.md`는 작업 중 매번 수정하지 말고 기능/작업 단위가 끝난 시점에 한 번만 반영한다.
8. 단순 UI 변경에는 과도한 테스트 파일을 새로 만들지 않는다 — 필요하면 기존 verify 파일에
   케이스를 추가하거나, 정말 새 기능일 때만 하나로 묶어 새 파일을 만든다.
9. 기존 기능에 직접 영향을 주지 않는 변경은 최소 범위로 수정한다.
10. 최종적으로 변경 파일 / 테스트 결과 / 남은 문제만 간단히 보고한다(2-1번 형식과 동일).
11. 테스트용 verify 파일을 기능마다 새로 만들지 않는다. 기존 테스트에 통합할 수 있으면 통합하고,
    임시 검증 스크립트는 작업 완료 후 삭제한다. (장기적으로는 `verify-calendar` / `verify-market` /
    `verify-photo` / `verify-app` 처럼 영역별로 3~5개만 유지하는 것을 목표로 한다 — 새 verify 파일을
    또 만들어야 할 것 같으면, 먼저 기존 영역별 파일에 편입할 수 있는지부터 검토한다.)

## 3. 작업 마감 자동화 규칙

아래 표현은 전부 "DeskPad 작업 마감" 명령으로 해석한다:
"오늘 작업 마감" / "업무 마감" / "오늘 업무 종료" / "작업 종료" / "오늘은 여기까지"

이 명령을 받으면 아래 순서를 전부 수행한다.

### 3-1. 현재 프로젝트 상태 확인
- `git status`로 변경된 파일을 확인한다.
- 의도하지 않은 파일, 민감정보로 보이는 파일이 섞여 있지 않은지 확인한다.

### 3-2. 보안 확인 (실패 시 커밋/푸시 중단)
다음이 Git에 포함(스테이징/커밋 대상)되지 않았는지 반드시 확인한다:
`.env`(및 `.env.*`, `.env.example` 제외), Google Client Secret, OAuth token, refresh token, API Key, 그 외 credential류.
하나라도 발견되면 **commit/push를 중단하고 사용자에게 즉시 알린다** — 다음 단계로 넘어가지 않는다.

### 3-3. 코드 검증
- `npm run build` (TypeScript 에러 확인)
- Netlify Functions를 건드렸다면 `npx tsc --noEmit -p netlify/functions/tsconfig.json`
- 이번 작업과 관련된 테스트/기존 자동 테스트가 있으면 필요한 범위에서 실행

### 3-4. 오류 처리
- build/test 오류가 나면 원인을 조사해 수정하고 다시 검증한다.
- **검증이 실패한 상태에서는 절대로 GitHub에 push하지 않는다.**

### 3-5. 작업 기록
- `roadmap.md`(project memory)가 있으면 오늘 완료한 내용을 기존 형식 그대로 이어서 기록한다: 완료 항목 / 변경 이유 / 주요 구현 내용 / 검증 결과 / 아직 남은 작업 / 다음 작업 권장 순서. 간결하게.

### 3-6. Git commit
- `git add` 전에 변경 파일 목록을 다시 한번 확인한다(3-2 보안 확인 재확인 포함).
- 작업 내용에 맞는 구체적인 커밋 메시지를 작성한다. `"Update"` 같은 의미 없는 메시지는 쓰지 않는다.

### 3-7. Git push
- 현재 브랜치가 `main`인지 확인한다.
- `origin main`으로 push한다.
- push 성공 여부를 확인한다.

### 3-8. Netlify
- GitHub push가 성공하면 Netlify의 `main` 브랜치 자동 배포 구조를 그대로 이용한다 — 필요하지 않은 한 Netlify 수동 배포는 실행하지 않는다.
- 가능하면 자동 배포가 시작될 수 있는 상태인지(=push 성공)만 확인한다.
- Netlify 인증정보나 환경변수 값은 절대 출력하지 않는다.

### 3-9. 최종 보고 형식
작업 마감이 끝나면 장황한 개발 로그 대신 아래 형식으로만 정리해서 보고한다:

```
오늘 완료
- 주요 작업

검증
- build 결과
- test 결과

Git
- commit message
- push 성공 여부

배포
- Netlify 자동 배포 대상 여부

다음 작업
- 다음에 이어서 할 핵심 작업 1~3개
```

## 4. 가벼운 정리 모드 ("일정 마감" / "오늘 할 일 정리")

사용자가 "일정 마감" 또는 "오늘 할 일 정리"라고 말하면 — **Git push는 하지 않고** 다음만 정리해서 보고한다:
1. 오늘 완료한 작업
2. 미완료 작업
3. 다음 작업 우선순위
4. `roadmap.md` 업데이트가 필요한지 여부

**"오늘 작업 마감"이라고 명시적으로 말했을 때만** 3번의 코드 검증 → commit → push까지 전체 마감 절차를 수행한다. "일정 마감"/"오늘 할 일 정리"는 절대 push까지 진행하지 않는다.
